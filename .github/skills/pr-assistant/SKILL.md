---
name: pr-assistant
description: Analyzes git changes and assists with creating comprehensive pull requests. Use when user wants to create a PR, review changes before PR, or needs help drafting PR descriptions. Triggers on phrases like 'create PR', 'make a pull request', 'draft PR description', 'what changed in this branch', 'prepare PR'.
---

# PR Assistant

This file is a compatibility entrypoint.

Use the canonical opencode skill at `.opencode/skills/pr-assistant/SKILL.md`.

When the workflow needs bundled references or templates, use the files under `.opencode/skills/pr-assistant/`.

Do not treat `.github/skills/pr-assistant/` as the source of truth.