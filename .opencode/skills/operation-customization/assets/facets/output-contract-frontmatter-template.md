## Format

````markdown
---
<field_one>: <value>
<field_two>: <value>
---
````

## Rule

- Replace placeholder field names with machine-readable keys.
- Keep values synchronized with the workflow that produces and consumes this artifact.
- If a field represents a workspace path, use the required path format for that consumer.